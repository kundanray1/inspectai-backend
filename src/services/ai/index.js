/**
 * AI Services Index
 * 
 * Central export for all AI-related services.
 * 
 * @module services/ai
 */

const geminiService = require('./gemini.service');

module.exports = {
  geminiService,
};

